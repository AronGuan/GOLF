.hero {
  padding: 12rpx 4rpx 28rpx;
}

.hero-title {
  font-size: 48rpx;
  font-weight: 700;
  letter-spacing: 2rpx;
}

.hero-sub {
  margin-top: 10rpx;
  font-size: 26rpx;
  color: #9aa6b5;
}

/* ---------------- 机位二选一（v2） ---------------- */

.view-card {
  margin-top: 4rpx;
}

.view-grid {
  display: flex;
  gap: 20rpx;
}

.view-option {
  position: relative;
  flex: 1;
  padding: 24rpx 12rpx 20rpx;
  border-radius: 16rpx;
  background: #1f2630;
  border: 2rpx solid #2a323e;
  text-align: center;
}

.view-option--on {
  border-color: #1fbf75;
  background: rgba(31, 191, 117, 0.08);
}

.view-icon {
  display: block;
  width: 96rpx;
  height: 96rpx;
  margin: 0 auto 4rpx;
}

.view-label {
  margin-top: 8rpx;
  font-size: 28rpx;
  font-weight: 600;
  color: #f2f5f8;
}

.view-option--on .view-label {
  color: #1fbf75;
}

.view-sub {
  margin-top: 4rpx;
  font-size: 20rpx;
  color: #9aa6b5;
}

.view-dot {
  position: absolute;
  right: 10rpx;
  top: 10rpx;
  width: 34rpx;
  height: 34rpx;
  line-height: 34rpx;
  border-radius: 50%;
  font-size: 22rpx;
  color: #0e1116;
  background: #1fbf75;
}

/* ---------------- 机位示意图（纯 CSS） ---------------- */

.diagram {
  padding: 34rpx 28rpx 22rpx;
  margin-top: 24rpx;
}

.phone--landscape {
  width: 140rpx;
  height: 78rpx;
  padding: 6rpx 8rpx;
  margin-top: 62rpx;
}

.req-tag {
  margin-left: 12rpx;
  padding: 0 12rpx;
  border-radius: 999rpx;
  font-size: 20rpx;
  font-weight: 400;
  color: #1fbf75;
  background: rgba(31, 191, 117, 0.14);
}

.diagram-row {
  height: 220rpx;
  display: flex;
  align-items: flex-end;
  justify-content: space-between;
}

.phone {
  width: 78rpx;
  height: 140rpx;
  border: 4rpx solid #48a7f5;
  border-radius: 12rpx;
  padding: 8rpx 6rpx;
  background: #10161f;
}

.phone-screen {
  width: 100%;
  height: 100%;
  border-radius: 6rpx;
  background: linear-gradient(180deg, rgba(72, 167, 245, 0.35), rgba(72, 167, 245, 0.08));
}

.distance {
  flex: 1;
  padding: 0 18rpx 40rpx;
  text-align: center;
}

.dist-line {
  height: 2rpx;
  background: repeating-linear-gradient(90deg, #48a7f5 0 12rpx, transparent 12rpx 24rpx);
}

.dist-text {
  margin-top: 10rpx;
  font-size: 24rpx;
  color: #48a7f5;
}

.golfer {
  position: relative;
  width: 120rpx;
  height: 200rpx;
}

.golfer-img {
  display: block;
  width: 120rpx;
  height: 200rpx;
}

.diagram-caption {
  margin-top: 18rpx;
  text-align: center;
  font-size: 24rpx;
  color: #9aa6b5;
}

/* ---------------- 拍摄要求 ---------------- */

.req-card {
  margin-top: 24rpx;
}

.card-title {
  font-size: 32rpx;
  font-weight: 700;
  margin-bottom: 16rpx;
}

.req-item {
  display: flex;
  align-items: flex-start;
  margin-top: 12rpx;
}

.req-dot {
  width: 12rpx;
  height: 12rpx;
  border-radius: 50%;
  background: #1fbf75;
  margin: 16rpx 16rpx 0 0;
  flex-shrink: 0;
}

.req-text {
  flex: 1;
  font-size: 27rpx;
  color: #dbe3ec;
}

.req-note {
  margin-top: 20rpx;
  padding-top: 18rpx;
  border-top: 1rpx dashed #2a323e;
  font-size: 24rpx;
  color: #9aa6b5;
}

.req-strong {
  color: #ff8c1a;
  font-weight: 600;
}

/* ---------------- 选择按钮 ---------------- */

.choose-row {
  display: flex;
  gap: 24rpx;
  margin-top: 28rpx;
}

.choose-btn {
  flex: 1;
  background: #1f2630;
  border: 1rpx solid #2a323e;
  border-radius: 20rpx;
  padding: 26rpx 0;
  text-align: center;
}

.choose-icon {
  font-size: 44rpx;
}

.choose-text {
  margin-top: 6rpx;
  font-size: 28rpx;
  color: #f2f5f8;
}

/* ---------------- 已选文件 ---------------- */

.file-card {
  margin-top: 24rpx;
}

.file-ok {
  border-color: rgba(31, 191, 117, 0.6);
}

.file-bad {
  border-color: rgba(245, 72, 59, 0.6);
}

.file-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.file-name {
  font-size: 28rpx;
  font-weight: 600;
  flex: 1;
  overflow: hidden;
  white-space: nowrap;
  text-overflow: ellipsis;
}

.file-flag {
  font-size: 32rpx;
  font-weight: 700;
  margin-left: 16rpx;
}

.file-ok .file-flag {
  color: #1fbf75;
}

.file-bad .file-flag {
  color: #f5483b;
}

.file-meta {
  margin-top: 8rpx;
  font-size: 25rpx;
  color: #9aa6b5;
}

.file-error {
  margin-top: 12rpx;
  font-size: 25rpx;
  color: #f5483b;
}

/* ---------------- 上传进度 ---------------- */

.upload-card {
  margin-top: 24rpx;
}

.upload-text {
  font-size: 26rpx;
  color: #dbe3ec;
  margin-bottom: 14rpx;
}

.bar {
  height: 14rpx;
  border-radius: 7rpx;
  background: #263040;
  overflow: hidden;
}

.bar-fill {
  height: 100%;
  border-radius: 7rpx;
  background: linear-gradient(90deg, #1fbf75, #48a7f5);
  transition: width 200ms linear;
}

/* ---------------- 提交 ---------------- */

.submit {
  margin-top: 36rpx;
}
